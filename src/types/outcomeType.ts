export type OutcomeRes = {
  id: string;
  userId: string;
  category: string;
  amount: number;
  name: string;
  date: string; // ISO date string

  createdAt: string; // ISO date string
  updatedAt: string; // ISO date string
};
